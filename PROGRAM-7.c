#include<stdio.h>
int main()
{
    int num,rem,sum=0,n;
    printf("enter the number ");
    scanf("%d",&num);
    if(n>0)
    {
        num=n%10;
        sum=sum+rem;
        num=n/10;
    printf("palindrome",sum);
    }
    else
    printf("not palindrome",sum);
}
